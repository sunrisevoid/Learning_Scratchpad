# AtBS Notes

== operator asks if two values are the same as each other, whereas
= assigns the value on the right to the left

*Boolean values* - only two values, **True** or **False**
*Comparison operators* - always two characters; ==, <=, !=, etc;
*Boolean operators* - only three: **and**, **or**, and **not**.

Flow control statements
often start with a part called the *condition*, which is followed by the *clause*
**if** is the condition, **else** is the clause.

**break** stops a loop from continuing, whereas **continue** repeats the loop.

**ASCII** means American Standard Code for Information Interchange

~~~
Practice Questions for chapter 1:
1. Operators: * - / + Values: 'hello' -88.8 5
2. Variable: spam String: 'spam'
3. Operators, Values, Strings- WRONG it is integers, floating point numbers, and strings.
4. An expression is made up of values and operators, expressions act on the values- ALMOST CORRECT expressions evaluate to a single value
5. An expression has a relationship, a statement is single WRONG an expression evaluates to a single value, a statement does not.
6. 21 WRONG there was no reassignment to bacon
7. spamspamspam
8. because 100 starts with a number
9. int(), str(), float()
10. Causes an error because it attempts to cat two strings with an integer, you fix it by converting the integer into a string with str(99) 
~~~

range(start, up-to-but-not-including, interval)
